# Dataset (not committed)

Put `Heart_diasease_dataset_from_Northern_Bangladesh.xlsx` in this folder to train or to run the
dataset tests. It holds patient-level hospital records, so it is git-ignored and must never be pushed.
